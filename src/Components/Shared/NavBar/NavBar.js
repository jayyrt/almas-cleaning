

const NavBar = ({ links, toggleMenu, isMenuOpen }) => {
    const menuClass = isMenuOpen ? 'menu-open' : '';

    return(
        <div>
            <nav className="nav-bar">
                        
            </nav>
        </div>
    )
}